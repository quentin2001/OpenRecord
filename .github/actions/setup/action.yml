name: Setup Node.js
description: Install Node 22, restore npm cache, run npm ci
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        node-version: 22
        cache: npm
    - run: npm ci
      shell: bash
